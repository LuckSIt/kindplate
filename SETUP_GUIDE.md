# 🚀 Руководство по запуску KindPlate

## 📌 Текущее состояние проекта

### ✅ Что работает:
- ✅ Главная страница `/`
- ✅ Страница с картой `/home` (с mock данными)
- ✅ Регистрация бизнеса `/auth/register/business`
- ✅ Регистрация клиента `/auth/register/customer`  
- ✅ Вход `/auth/login`
- ✅ Backend API на порту 5000

### ⚠️ Требует настройки:
- ⚠️ База данных PostgreSQL
- ⚠️ Панель управления бизнесом `/panel`
- ⚠️ Страница аккаунта `/account`

## 🔧 Быстрый старт (с mock данными)

### 1. Запустите backend:
```bash
cd backend
npm run dev
```

### 2. Запустите frontend:
```bash
cd frontend  
npm run dev
```

### 3. Откройте браузер:
```
http://localhost:3000
```

## 🗄️ Настройка PostgreSQL (для полного функционала)

### 1. Установите PostgreSQL:
- Скачайте с https://www.postgresql.org/download/
- Запустите установку
- Запомните пароль для пользователя `postgres`

### 2. Создайте файл `.env` в папке `backend`:
```env
PORT=5000
SECRET_KEY=kindplate-secret-key-2024
DB_USER=postgres
DB_HOST=localhost
DB_NAME=kindplate
DB_PASSWORD=ваш_пароль_от_postgres
DB_PORT=5432
```

### 3. Запустите скрипт пересоздания БД:
```bash
cd backend
node recreate-database.js
```

Скрипт автоматически:
- ✅ Удалит старую БД (если есть)
- ✅ Создаст новую БД `kindplate`
- ✅ Создаст все таблицы (users, items, offers, orders, favorites, reviews)
- ✅ Добавит тестовые данные

### 4. Включите настоящий API:
Откройте `backend/src/index.js` и замените:
```javascript
const customerRouter = require("./routes/customer-simple");
```
на:
```javascript
const customerRouter = require("./routes/customer");
```

### 5. Перезапустите backend:
```bash
npm run dev
```

## 📱 Тестовые данные

После настройки БД будут доступны:

**Бизнес аккаунт:**
- Email: `cafe@test.com`
- Пароль: `123456`

**Клиент:**
- Email: `customer@test.com`
- Пароль: `123456`

## 🐛 Известные проблемы

### 1. Ошибка "Cannot read properties of undefined"
**Решение:** Обновите страницу (F5)

### 2. "NOT_AUTHENTICATED" в панели
**Решение:** Войдите в аккаунт бизнеса

### 3. Пустая карта
**Решение:** 
- Проверьте, что backend запущен
- Проверьте консоль браузера (F12)
- Убедитесь, что mock API возвращает данные

## 📚 Структура проекта

```
kplate-master/
├── backend/
│   ├── src/
│   │   ├── index.js              # Главный файл backend
│   │   ├── routes/
│   │   │   ├── auth.js           # Авторизация
│   │   │   ├── business.js       # API для бизнеса
│   │   │   ├── customer.js       # API для клиентов (с БД)
│   │   │   └── customer-simple.js # Mock API
│   │   └── lib/
│   │       ├── auth.js           # Middleware авторизации
│   │       └── db.js             # Подключение к БД
│   ├── init.sql                  # Схема БД
│   └── recreate-database.js      # Скрипт настройки БД
├── frontend/
│   ├── src/
│   │   ├── routes/
│   │   │   ├── __root.tsx        # Корневой layout
│   │   │   ├── index.tsx         # Главная страница
│   │   │   ├── home/             # Карта с предложениями
│   │   │   ├── panel/            # Панель бизнеса
│   │   │   ├── account/          # Аккаунт пользователя
│   │   │   └── auth/             # Регистрация и вход
│   │   └── lib/
│   │       └── axiosInstance.ts  # Конфигурация API
│   └── .env                      # Настройки frontend
└── SETUP_GUIDE.md               # Это руководство
```

## 🆘 Помощь

Если что-то не работает:
1. Проверьте консоль браузера (F12)
2. Проверьте логи backend
3. Убедитесь, что оба сервера запущены
4. Проверьте, что порты 3000 и 5000 свободны
