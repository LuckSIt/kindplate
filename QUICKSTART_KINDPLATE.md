# 🚀 KindPlate - Быстрый старт

## Что нужно для запуска:

1. **PostgreSQL** 14+ (установлен и запущен)
2. **Node.js** 18+ и npm
3. **Git** (для клонирования репозитория)

---

## 📦 Шаг 1: Настройка Backend

### 1.1. Перейдите в папку backend
```bash
cd backend
```

### 1.2. Установите зависимости
```bash
npm install
```

### 1.3. Создайте `.env` файл
Скопируйте `env-example.txt` в `.env` и заполните:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=YOUR_POSTGRES_PASSWORD
DB_NAME=kindplate
PORT=5000
SESSION_SECRET=some_random_secret_here
```

**Важно:** Замените `YOUR_POSTGRES_PASSWORD` на ваш реальный пароль от PostgreSQL!

### 1.4. Пересоздайте базу данных
```bash
npm run db:recreate
```

Это создаст:
- БД `kindplate`
- Все таблицы (users, partners, offers, orders, payments, etc.)
- Тестовые данные (4 пользователя + 3 партнёра + 6 предложений)

### 1.5. Запустите backend
```bash
npm run dev
```

✅ Backend запущен на **http://localhost:5000**

Проверьте: откройте в браузере `http://localhost:5000/health`

---

## 🎨 Шаг 2: Настройка Frontend

### 2.1. Откройте новый терминал и перейдите в папку frontend
```bash
cd frontend
```

### 2.2. Установите зависимости
```bash
npm install
```

### 2.3. Создайте `.env` файл
Скопируйте `env.example` в `.env`:

```env
VITE_BACKEND_BASE_URL=http://localhost:5000
```

### 2.4. Запустите frontend
```bash
npm run dev
```

✅ Frontend запущен на **http://localhost:3000** (или 5173)

---

## 🔑 Тестовые аккаунты

После выполнения `npm run db:recreate` доступны следующие аккаунты:

### Клиент:
- **Email:** `customer@test.com`
- **Пароль:** `123456`

### Партнёры:
1. **Кафе Цезарь**
   - Email: `partner1@test.com`
   - Пароль: `123456`

2. **Пекарня Уют**
   - Email: `partner2@test.com`
   - Пароль: `123456`

3. **Салат Бар**
   - Email: `partner3@test.com`
   - Пароль: `123456`

---

## 🛠️ Возможные проблемы

### Проблема 1: "Database connection failed"
**Решение:**
1. Убедитесь, что PostgreSQL запущен:
   ```bash
   # Windows
   pg_ctl status
   
   # Linux/Mac
   pg_isready
   ```
2. Проверьте пароль в `.env`
3. Убедитесь, что пользователь `postgres` существует

### Проблема 2: "Port 5000 is already in use"
**Решение:**
1. Измените порт в `backend/.env`: `PORT=5001`
2. Обновите `frontend/.env`: `VITE_BACKEND_BASE_URL=http://localhost:5001`

### Проблема 3: "Cannot find module 'uuid'"
**Решение:**
```bash
cd backend
npm install
```

### Проблема 4: Стили не применяются на frontend
**Решение:**
1. Очистите кеш браузера (Ctrl+Shift+R)
2. Перезапустите frontend dev server

---

## 📚 Дополнительная документация

- **Backend API:** См. `backend/README.md`
- **Database Schema:** См. `backend/init.sql`
- **Technical Spec:** См. `TECH_SPEC_KINDPLATE.md`

---

## 🎯 Следующие шаги

1. **Войдите** как клиент (`customer@test.com` / `123456`)
2. **Просмотрите** карту с предложениями на `/home`
3. **Протестируйте** создание заказа
4. **Войдите** как партнёр (`partner1@test.com` / `123456`)
5. **Управляйте** предложениями в `/panel`

---

## 💡 Полезные команды

### Backend:
```bash
npm run dev          # Запуск с hot-reload
npm start            # Production запуск
npm run db:recreate  # Пересоздать БД
```

### Frontend:
```bash
npm run dev          # Запуск dev сервера
npm run build        # Production build
npm run preview      # Просмотр production build
```

### PostgreSQL:
```bash
# Подключиться к БД
psql -U postgres -d kindplate

# Посмотреть таблицы
\dt

# Посмотреть пользователей
SELECT * FROM users;

# Посмотреть предложения
SELECT * FROM offers;
```

---

## 🚀 Готово!

Проект KindPlate запущен и готов к разработке! 🎉

Если возникли проблемы, проверьте логи в терминалах backend и frontend.



