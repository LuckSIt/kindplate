# ✅ Чеклист для проверки обновления на сервере

## 🔍 Диагностика проблемы "ничего не поменялось"

### 1. Проверьте, что код обновился на сервере

**На сервере (SSH):**
```bash
# Подключитесь к серверу
ssh your-user@your-server

# Перейдите в директорию проекта
cd /path/to/kplate-master

# Проверьте последний коммит
git log -1

# Проверьте, что файл обновился
grep -n "LandingPage" frontend/src/routes/index.tsx
# Должно показать: import { LandingPage } from "@/components/pages/LandingPage";
```

---

### 2. Проверьте, что Docker контейнер пересобрался

```bash
# Проверьте статус контейнеров
docker compose ps

# Проверьте логи frontend
docker compose logs --tail=50 frontend | grep -i "build\|error\|success"

# Проверьте, когда был собран образ
docker images | grep kindplate-frontend
```

---

### 3. Принудительная пересборка (если нужно)

```bash
# Остановите frontend
docker compose stop frontend

# Удалите старый образ
docker rmi $(docker images | grep kindplate-frontend | awk '{print $3}') 2>/dev/null || true

# Пересоберите БЕЗ кеша
docker compose build --no-cache frontend

# Запустите
docker compose up -d frontend

# Проверьте логи
docker compose logs -f frontend
```

---

### 4. Очистка кеша браузера

**Важно!** Браузер может кешировать старую версию:

1. **Жесткая перезагрузка:**
   - Windows/Linux: `Ctrl + Shift + R` или `Ctrl + F5`
   - Mac: `Cmd + Shift + R`

2. **Режим инкогнито:**
   - Откройте сайт в режиме инкогнито/приватном режиме

3. **Очистка кеша:**
   - Chrome: Настройки → Конфиденциальность → Очистить данные → Кешированные изображения и файлы
   - Firefox: Настройки → Приватность → Очистить данные

---

### 5. Проверьте, что вы на правильной странице

Убедитесь, что открыта главная страница `/`, а не `/home`:

- ✅ Правильно: `https://app.kindplate.ru/`
- ❌ Неправильно: `https://app.kindplate.ru/home`

---

### 6. Проверьте версию в браузере

Откройте консоль разработчика (F12) и выполните:

```javascript
// Проверьте, что загрузился правильный файл
fetch('/assets/index-*.js').then(r => r.text()).then(console.log)

// Или проверьте Network tab → посмотрите на загруженные JS файлы
// Убедитесь, что они свежие (не из кеша)
```

---

### 7. Проверьте, что изменения в коде

**Локально проверьте файлы:**

```bash
# Проверьте routes/index.tsx
cat frontend/src/routes/index.tsx
# Должно быть: import { LandingPage } from "@/components/pages/LandingPage";

# Проверьте, что компоненты существуют
ls -la frontend/src/components/landing/
ls -la frontend/src/components/pages/LandingPage.tsx
```

---

### 8. Если используете Амвера

Проверьте в панели Амвера:
1. Откройте проект в панели Амвера
2. Перейдите в "Deployments"
3. Проверьте последний деплой - он должен быть успешным
4. Проверьте логи последнего деплоя

---

### 9. Если изменения все еще не видны

**Полная пересборка всего проекта:**

```bash
# Остановите все
docker compose down

# Обновите код
git pull origin main

# Пересоберите все БЕЗ кеша
docker compose build --no-cache

# Запустите все
docker compose up -d

# Проверьте статус
docker compose ps

# Проверьте логи
docker compose logs -f
```

---

### 10. Проверка через curl (без кеша браузера)

```bash
# Проверьте, что сервер отдает правильный HTML
curl -H "Cache-Control: no-cache" https://app.kindplate.ru/ | grep -i "landing\|выгодно"

# Должно найти текст из новой лендинг страницы
```

---

## 🎯 Быстрое решение

Если ничего не помогает, выполните полную пересборку:

```bash
# 1. Обновите код
git pull origin main

# 2. Полная пересборка
docker compose down
docker compose build --no-cache
docker compose up -d

# 3. Очистите кеш браузера и проверьте
```

---

## 📝 Что должно быть видно после обновления

На главной странице `/` должно быть:

✅ Логотип KindPlate с листочком  
✅ Заголовок "Выгодно для тебя, полезно для планеты"  
✅ Кнопки "начать спасать" и "начать продавать"  
✅ Статистика "Вместе мы спасли 532 блюд от выброса"  
✅ Карточки "Для пользователей"  
✅ Секция "Почему KindPlate?"  
✅ Футер  

❌ НЕ должно быть:
- Поисковой строки вверху (на главной странице)
- Нижней навигации (на главной странице)

