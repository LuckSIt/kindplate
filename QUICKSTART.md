# ⚡ Быстрый старт KindPlate

## 🚀 За 3 минуты (с mock данными)

### 1️⃣ Запустите backend:
```bash
cd backend
npm run dev
```

### 2️⃣ Запустите frontend:
```bash
cd frontend
npm run dev
```

### 3️⃣ Откройте браузер:
```
http://localhost:3000
```

**✅ Готово!** Карта с предложениями работает с тестовыми данными.

---

## 🗄️ Полная настройка (с PostgreSQL)

### Шаг 1: Создайте `.env` файл

В папке `backend` создайте файл `.env`:

```env
PORT=5000
SECRET_KEY=kindplate-secret-key-2024
DB_USER=postgres
DB_HOST=localhost
DB_NAME=kindplate
DB_PASSWORD=ваш_пароль
DB_PORT=5432
```

### Шаг 2: Запустите скрипт

```bash
cd backend
node recreate-database.js
```

### Шаг 3: Включите настоящее API

В файле `backend/src/index.js` **строка 12** замените:

**Было:**
```javascript
const customerRouter = require("./routes/customer-simple");
```

**Стало:**
```javascript
const customerRouter = require("./routes/customer");
```

### Шаг 4: Перезапустите серверы

```bash
# Backend
cd backend
npm run dev

# Frontend
cd frontend
npm run dev
```

---

## 🔐 Тестовые аккаунты

**Бизнес:**
- Email: `cafe@test.com`
- Пароль: `123456`

**Клиент:**
- Email: `customer@test.com`
- Пароль: `123456`

---

## 📋 Что работает сейчас?

### ✅ С mock данными (без PostgreSQL):
- Главная страница
- Карта с предложениями
- Регистрация (не сохраняется)
- Вход (не работает)

### ✅ С PostgreSQL:
- **Всё выше +**
- Регистрация бизнеса (сохраняется в БД)
- Регистрация клиента (сохраняется в БД)
- Вход в аккаунт
- Панель управления бизнесом `/panel`
- Добавление/редактирование товаров
- Страница аккаунта `/account`

---

## 🐛 Проблемы?

### "Cannot read properties of undefined"
→ **Решение:** Обновите страницу (F5)

### "NOT_AUTHENTICATED"
→ **Решение:** Войдите в аккаунт

### База данных не подключается
→ **Решение:** 
1. Проверьте пароль в `.env`
2. Убедитесь, что PostgreSQL запущен
3. Запустите `node recreate-database.js` еще раз

---

## 📚 Полная документация

Смотрите **SETUP_GUIDE.md** для подробной информации.

