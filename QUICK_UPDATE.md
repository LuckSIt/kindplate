# 🚀 Быстрое обновление на сервере

## ⚡ Самый быстрый способ (Амвера)

```bash
# 1. Закоммитьте изменения
git add .
git commit -m "Обновление: новая лендинг страница без хедера на главной"
git push origin main

# 2. Подождите 3-5 минут - Амвера автоматически обновит

# 3. ОЧИСТИТЕ КЕШ БРАУЗЕРА:
#    - Нажмите Ctrl+Shift+R (Windows/Linux) или Cmd+Shift+R (Mac)
#    - ИЛИ откройте в режиме инкогнито
```

---

## 🔧 Если автоматический деплой не сработал

### Подключитесь к серверу по SSH:

```bash
ssh your-user@your-server
cd /path/to/kplate-master
```

### Выполните команды:

```bash
# 1. Обновите код
git pull origin main

# 2. Пересоберите frontend БЕЗ кеша
docker compose build --no-cache frontend

# 3. Перезапустите frontend
docker compose up -d frontend

# 4. Проверьте логи
docker compose logs --tail=30 frontend
```

---

## ✅ Что должно измениться

После обновления на главной странице `/`:

✅ **НЕТ** хедера с логотипом сверху  
✅ **НЕТ** поисковой строки  
✅ **НЕТ** нижней навигации (Карта/Профиль)  
✅ **ЕСТЬ** полная лендинг-страница с:
   - Заголовком "Выгодно для тебя, полезно для планеты"
   - Кнопками "начать спасать" и "начать продавать"
   - Статистикой
   - Карточками для пользователей
   - Секцией "Почему KindPlate?"
   - Футером

---

## 🐛 Если все еще не работает

1. **Очистите кеш браузера:**
   - Chrome: `Ctrl+Shift+Delete` → выберите "Кешированные изображения и файлы"
   - Или откройте в режиме инкогнито

2. **Проверьте, что вы на правильной странице:**
   - Должно быть: `https://app.kindplate.ru/` (слеш в конце)
   - НЕ должно быть: `https://app.kindplate.ru/home`

3. **Проверьте логи на сервере:**
   ```bash
   docker compose logs -f frontend
   ```

4. **Принудительная пересборка:**
   ```bash
   docker compose down frontend
   docker compose build --no-cache frontend
   docker compose up -d frontend
   ```

---

## 📋 Проверка изменений в коде

Убедитесь, что в файле `frontend/src/routes/__root.tsx` есть строка:

```typescript
location.pathname === '/';
```

Это скрывает хедер и навигацию на главной странице.

