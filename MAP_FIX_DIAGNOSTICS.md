# 🔧 ИСПРАВЛЕНО: Проблемы с Яндекс картой

## Что было исправлено

### 1. ✅ Добавлено детальное логирование
Теперь в консоли браузера вы увидите:
- `⏳ Ожидание загрузки Yandex Maps API...`
- `✅ Yandex Maps API загружен`
- `🗺️ Инициализация карты...`
- `✅ Карта успешно создана`
- Или ошибки с подробным описанием

### 2. ✅ Исправлена проверка размеров контейнера
Добавлена проверка размеров контейнера перед инициализацией карты. Если размеры нулевые, они устанавливаются явно.

### 3. ✅ Добавлены явные стили для контейнера
- `width: 100%`
- `height: 100%`
- `minHeight: 400px`

### 4. ✅ Улучшены индикаторы загрузки
Теперь индикаторы загрузки показываются прямо в контейнере карты, а не заменяют его.

### 5. ✅ Исправлена структура контейнеров
Добавлены явные стили в `home/index.tsx` для правильного отображения карты.

## Как проверить что карта работает

### Шаг 1: Откройте консоль браузера (F12)

Вы должны увидеть логи:
```
⏳ Ожидание загрузки Yandex Maps API...
✅ Yandex Maps API загружен
🗺️ Инициализация карты... { containerSize: { width: ..., height: ... }, center: [...] }
✅ Карта успешно создана
```

### Шаг 2: Проверьте что Yandex Maps API загружен

В консоли выполните:
```javascript
console.log('Yandex Maps:', typeof window.ymaps);
```

**Должно вывести:** `Yandex Maps: object`

Если выводит `undefined` - проблема в загрузке скрипта из `index.html`.

### Шаг 3: Проверьте размеры контейнера

В консоли выполните:
```javascript
const mapContainer = document.querySelector('[ref]') || document.querySelector('.w-full.h-full');
if (mapContainer) {
    const rect = mapContainer.getBoundingClientRect();
    console.log('Размеры контейнера карты:', { width: rect.width, height: rect.height });
} else {
    console.error('Контейнер карты не найден!');
}
```

**Должно вывести:** Размеры больше 0 (например, `{ width: 402, height: 600 }`)

### Шаг 4: Проверьте что данные загружаются

В консоли выполните:
```javascript
// Проверьте что businesses передаются в MapView
// Это можно увидеть в React DevTools или в логах компонента
```

## Возможные проблемы и решения

### Проблема 1: "Yandex Maps API не загружен"

**Причина:** Скрипт не загружается из `index.html`

**Решение:**
1. Проверьте `frontend/index.html` - должна быть строка:
```html
<script src="https://api-maps.yandex.ru/2.1.79/?apikey=5389a46e-df33-4fe4-9077-69c18437eec5&lang=ru_RU"></script>
```

2. Проверьте что API ключ валидный (может быть заблокирован или истек)

3. Проверьте Network tab в DevTools - запрос к `api-maps.yandex.ru` должен быть успешным (200)

### Проблема 2: "Контейнер карты имеет нулевые размеры"

**Причина:** CSS не применяется или контейнер скрыт

**Решение:**
1. Проверьте что родительский контейнер имеет `height: 100%` или фиксированную высоту
2. Проверьте что нет `display: none` на контейнере
3. Проверьте что нет `overflow: hidden` который скрывает карту

### Проблема 3: "Карта создана, но не видна"

**Причина:** Карта перекрыта другими элементами или имеет неправильный z-index

**Решение:**
1. Проверьте z-index элементов
2. Проверьте что bottom sheet (Drawer) не перекрывает карту полностью
3. Проверьте что нет элементов с `position: absolute` которые перекрывают карту

### Проблема 4: "Данные не загружаются"

**Причина:** Backend не отвечает или возвращает ошибку

**Решение:**
1. Проверьте что backend работает: `curl https://api-kindplate.ru/health`
2. Проверьте Network tab - запросы к API должны быть успешными
3. Проверьте что `filteredBusinesses` не пустой массив

## Диагностика в консоли браузера

Выполните этот код для полной диагностики:

```javascript
// 1. Проверка Yandex Maps API
console.log('1. Yandex Maps API:', typeof window.ymaps !== 'undefined' ? '✅ Загружен' : '❌ Не загружен');

// 2. Проверка контейнера карты
const mapContainer = document.querySelector('div[class*="w-full"][class*="h-full"]');
if (mapContainer) {
    const rect = mapContainer.getBoundingClientRect();
    console.log('2. Контейнер карты:', {
        найден: '✅',
        размеры: { width: rect.width, height: rect.height },
        видим: rect.width > 0 && rect.height > 0 ? '✅' : '❌'
    });
} else {
    console.log('2. Контейнер карты: ❌ Не найден');
}

// 3. Проверка скрипта Yandex Maps
const yandexScript = document.querySelector('script[src*="api-maps.yandex.ru"]');
console.log('3. Скрипт Yandex Maps:', yandexScript ? '✅ Найден' : '❌ Не найден');

// 4. Проверка ошибок в консоли
console.log('4. Проверьте вкладку Console на наличие ошибок (красные сообщения)');
```

## После исправлений

После деплоя исправлений:

1. **Очистите кэш браузера:** Ctrl+Shift+R (hard reload)
2. **Откройте консоль:** F12 → Console
3. **Проверьте логи:** Должны быть сообщения о загрузке карты
4. **Проверьте карту:** Должна отображаться карта Санкт-Петербурга

## Если карта все еще не работает

### Соберите информацию:

1. Скриншот консоли браузера (F12 → Console)
2. Скриншот Network tab (F12 → Network → фильтр по "yandex" или "api-kindplate")
3. Результат диагностики из консоли (код выше)

### Проверьте:

- [ ] Backend работает (`curl https://api-kindplate.ru/health`)
- [ ] Yandex Maps API загружается (Network tab)
- [ ] Контейнер карты имеет размеры > 0
- [ ] Нет ошибок в консоли
- [ ] Данные businesses загружаются (Network tab → offers/search)

## Файлы изменены

1. `frontend/src/components/ui/map-view.tsx` - добавлено логирование и проверки
2. `frontend/src/routes/home/index.tsx` - добавлены явные стили для контейнера

## Деплой

```bash
cd frontend
npm run build

docker compose down frontend
docker compose up -d --build frontend
docker compose restart caddy
```

После деплоя проверьте консоль браузера - там будут подробные логи о загрузке карты!

