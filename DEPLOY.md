# 📦 План деплоя на сервер Timeweb

## 📋 Список изменений

### Backend (новые файлы):
1. `backend/add_reviews_moderation.sql` - миграция для модерации отзывов
2. `backend/migrate-reviews-moderation.js` - скрипт миграции
3. `backend/add_quality_badges.sql` - миграция для бейджей качества
4. `backend/migrate-quality-badges.js` - скрипт миграции
5. `backend/src/middleware/review-photos-upload.js` - middleware для загрузки фото
6. `backend/src/jobs/quality-badges.js` - джоб обновления бейджей

### Backend (измененные файлы):
1. `backend/package.json` - добавлена зависимость `sharp@^0.33.2`
2. `backend/src/routes/reviews.js` - добавлена поддержка фото и модерации
3. `backend/src/routes/customer.js` - добавлены бейджи в ответы API
4. `backend/src/index.js` - добавлен запуск джоба бейджей

### Frontend (новые файлы):
1. `frontend/src/components/ui/quality-badge.tsx` - компонент бейджей

### Frontend (измененные файлы):
1. `frontend/src/components/ui/review-form.tsx` - загрузка фото
2. `frontend/src/components/ui/reviews-list.tsx` - отображение фото и статусов
3. `frontend/src/components/ui/offers-list.tsx` - отображение бейджей
4. `frontend/src/components/ui/vendor-header.tsx` - отображение бейджей
5. `frontend/src/components/pages/vendor-page.tsx` - секция с бейджами
6. `frontend/src/lib/schemas/review.ts` - обновлены типы
7. `frontend/src/lib/types.ts` - добавлено поле badges

---

## 🚀 Команды для деплоя

### 1. Подключение к серверу и переход в директорию проекта

```bash
# Подключитесь к серверу через SSH
ssh user@your-server-ip

# Перейдите в директорию проекта (замените на ваш путь)
cd /path/to/kplate-master
# или
cd ~/kplate-master
```

### 2. Получение изменений из Git

```bash
# Проверьте текущую ветку
git status

# Получите последние изменения (если они уже запушены)
git pull origin main
# или
git pull origin master
```

### 3. Backend: Установка зависимостей

```bash
cd backend

# Установка новых зависимостей (sharp для обработки изображений)
npm install

# Проверьте, что sharp установлен
npm list sharp
```

### 4. Backend: Запуск миграций БД

```bash
# Запустите миграцию для модерации отзывов
node migrate-reviews-moderation.js

# Запустите миграцию для бейджей качества
node migrate-quality-badges.js

# Проверьте, что миграции прошли успешно
# (должны быть сообщения об успешном завершении)
```

### 5. Backend: Создание директории для загрузки фото отзывов

```bash
# Создайте директорию для загрузки фото отзывов (если её нет)
mkdir -p uploads/reviews

# Установите правильные права доступа
chmod 755 uploads/reviews
```

### 6. Backend: Перезапуск сервиса

**Если используете Docker Compose:**
```bash
# Из корня проекта (где docker-compose.yml)
cd ..

# Пересоберите и перезапустите контейнер backend
docker-compose up -d --build backend

# Или перезапустите все сервисы
docker-compose down
docker-compose up -d --build

# Проверьте логи
docker-compose logs -f backend
```

**Если используете PM2 или systemd:**
```bash
# PM2
pm2 restart backend
pm2 logs backend

# systemd
sudo systemctl restart kindplate-backend
sudo systemctl status kindplate-backend
```

**Если запускаете напрямую через Node.js:**
```bash
# Остановите текущий процесс (Ctrl+C или kill)
# Затем запустите заново
npm start
# или
node src/index.js
```

### 7. Frontend: Установка зависимостей и сборка

```bash
cd ../frontend

# Установка зависимостей (если нужно)
npm install

# Сборка проекта
npm run build

# Проверьте, что сборка прошла успешно
# Должна создаться директория dist/
ls -la dist/
```

### 8. Frontend: Обновление статических файлов

**Если используете Nginx/Caddy:**
```bash
# Скопируйте собранные файлы в директорию веб-сервера
# (замените /var/www/kindplate на ваш путь)

sudo cp -r dist/* /var/www/kindplate/

# Или если используете символические ссылки
sudo rm -rf /var/www/kindplate/*
sudo cp -r dist/* /var/www/kindplate/
```

**Если используете Docker:**
```bash
# Пересоберите frontend контейнер
cd ..
docker-compose up -d --build frontend
```

### 9. Проверка работы

```bash
# Проверьте логи backend
docker-compose logs backend
# или
pm2 logs backend
# или
tail -f backend/logs/combined.log

# Проверьте, что джоб бейджей запущен (в логах должно быть):
# "✅ Джоб обновления бейджей качества запущен"

# Проверьте доступность API
curl http://localhost:YOUR_PORT/
```

### 10. Проверка функционала

1. **Модерация отзывов:**
   - Создайте отзыв с фото
   - Проверьте, что отзыв создается со статусом `pending`
   - Проверьте админ эндпоинт `/reviews/admin/pending`

2. **Бейджи качества:**
   - Проверьте, что бейджи отображаются на карточках продавцов
   - Проверьте страницу продавца - должна быть секция с бейджами
   - Проверьте, что джоб обновления бейджей работает (в логах)

---

## ⚙️ Переменные окружения

Убедитесь, что в `.env` файле backend есть следующие переменные:

```env
# База данных
DB_USER=your_db_user
DB_HOST=localhost
DB_NAME=kindplate
DB_PASSWORD=your_db_password
DB_PORT=5432

# Или используйте DATABASE_URL
# DATABASE_URL=postgresql://user:password@host:port/database

# Джобы (опционально, по умолчанию включены)
ENABLE_OFFER_SCHEDULER=true
ENABLE_QUALITY_BADGES_JOB=true
```

---

## 🔍 Проверка после деплоя

### Проверка миграций БД:

```bash
# Подключитесь к PostgreSQL
psql -U your_user -d kindplate

# Проверьте таблицу quality_badges
\dt quality_badges
\d quality_badges

# Проверьте таблицу reviews (должны быть новые поля)
\d reviews

# Проверьте функции
\df calculate_business_metrics
\df update_business_badges

# Выйти
\q
```

### Проверка файлов:

```bash
# Проверьте, что новые файлы на месте
ls -la backend/src/middleware/review-photos-upload.js
ls -la backend/src/jobs/quality-badges.js
ls -la frontend/src/components/ui/quality-badge.tsx

# Проверьте директорию загрузок
ls -la backend/uploads/reviews/
```

---

## 🐛 Troubleshooting

### Ошибка "Cannot find module 'sharp'"
```bash
cd backend
npm install sharp
# или
npm install --force
```

### Ошибка миграции БД
```bash
# Проверьте подключение к БД
psql -U your_user -d kindplate -c "SELECT 1;"

# Проверьте права доступа
# Убедитесь, что пользователь БД имеет права на создание таблиц и функций
```

### Джоб не запускается
```bash
# Проверьте логи
docker-compose logs backend | grep "quality-badges"
# или
pm2 logs | grep "quality-badges"

# Проверьте переменную окружения
echo $ENABLE_QUALITY_BADGES_JOB
```

### Фото не загружаются
```bash
# Проверьте права на директорию
ls -la backend/uploads/reviews/
chmod 755 backend/uploads/reviews/

# Проверьте, что директория существует
mkdir -p backend/uploads/reviews
```

---

## 📝 Примечания

1. **Миграции БД** запускайте в порядке зависимости:
   - Сначала `migrate-reviews-moderation.js`
   - Затем `migrate-quality-badges.js`

2. **Sharp** - это нативная библиотека, которая требует компиляции. 
   Убедитесь, что на сервере установлены build tools:
   ```bash
   # Ubuntu/Debian
   sudo apt-get install build-essential python3
   
   # CentOS/RHEL
   sudo yum groupinstall "Development Tools"
   sudo yum install python3
   ```

3. **Джоб бейджей** запускается автоматически каждый день в 3:00 ночи.
   Для тестирования можно вызвать функцию вручную через API или напрямую.

4. **Директория uploads** должна быть доступна для записи веб-сервером/приложением.

