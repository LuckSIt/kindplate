# 🔄 Как обновить данные на сервере

В зависимости от того, какой способ деплоя вы используете, следуйте соответствующим инструкциям:

---

## 📦 Вариант 1: Амвера (Docker Compose)

Если вы используете **Амвера** (файл `amvera.yml` присутствует), то есть два способа обновления:

### 🚀 Автоматическое обновление (рекомендуется)

Амвера автоматически деплоит изменения при push в ветку `main`:

```bash
# 1. Зафиксируйте изменения
git add .

# 2. Создайте коммит
git commit -m "Обновление: новая лендинг страница"

# 3. Отправьте в репозиторий
git push origin main
# или
git push amvera main
```

Амвера автоматически:
- Получит изменения
- Пересоберет Docker образы
- Перезапустит контейнеры
- Применит обновления

**Время обновления:** обычно 3-5 минут

---

### 🔧 Ручное обновление через SSH

Если автоматический деплой не работает, подключитесь к серверу по SSH:

```bash
# 1. Подключитесь к серверу
ssh your-user@your-server

# 2. Перейдите в директорию проекта
cd /path/to/kplate-master

# 3. Обновите код из Git
git pull origin main

# 4. Пересоберите и перезапустите только frontend
docker compose build --no-cache frontend
docker compose up -d frontend

# 5. Проверьте логи
docker compose logs -f frontend
```

Или используйте готовый скрипт:

```bash
./update-frontend.sh
```

---

### 🔄 Обновление всего проекта (backend + frontend)

```bash
# Остановите все контейнеры
docker compose down

# Обновите код
git pull origin main

# Пересоберите все без кеша
docker compose build --no-cache

# Запустите все контейнеры
docker compose up -d

# Проверьте статус
docker compose ps

# Посмотрите логи
docker compose logs -f
```

---

## 🚂 Вариант 2: Railway (Backend) + Vercel (Frontend)

### Backend на Railway

**Автоматически:**
Railway автоматически деплоит при push в `main`:

```bash
git add .
git commit -m "Обновление backend"
git push origin main
```

**Вручную:**
1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект → Backend сервис
3. Перейдите в "Deployments"
4. Нажмите "Deploy" → выберите ветку `main`
5. Дождитесь завершения деплоя

**Через Railway CLI:**
```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в аккаунт
railway login

# Подключитесь к проекту
railway link

# Деплой
railway up
```

---

### Frontend на Vercel

**Автоматически:**
Vercel автоматически деплоит при push в `main`:

```bash
git add .
git commit -m "Обновление frontend: новая лендинг страница"
git push origin main
```

**Вручную:**
1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в "Deployments"
4. Нажмите "..." → "Redeploy" или "Deploy"

**Через Vercel CLI:**
```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой production
vercel --prod
```

---

## 🔍 Проверка обновления

После обновления проверьте:

### 1. Проверьте логи

**Амвера/Docker:**
```bash
docker compose logs -f frontend
docker compose logs -f backend
```

**Railway:**
- Откройте проект → Backend → Logs

**Vercel:**
- Откройте проект → Deployments → выберите деплой → View Function Logs

---

### 2. Проверьте работу сайта

Откройте в браузере:
- Frontend: `https://app.kindplate.ru` (или ваш домен)
- Backend API: `https://api.kindplate.ru` (или ваш домен)

**Важно:** Очистите кеш браузера (Ctrl+Shift+R) или откройте в режиме инкогнито!

---

### 3. Проверьте новые изменения

Для проверки новой лендинг-страницы:
- Откройте главную страницу `/`
- Убедитесь, что отображаются новые компоненты
- Проверьте работу слайдера с блюдами

---

## 🐛 Решение проблем

### Обновление не применяется

1. **Очистите кеш браузера:**
   ```
   Ctrl + Shift + R (Windows/Linux)
   Cmd + Shift + R (Mac)
   ```

2. **Проверьте, что код действительно обновился:**
   ```bash
   # На сервере проверьте последний коммит
   git log -1
   
   # Проверьте содержимое файла
   cat frontend/src/routes/index.tsx | head -10
   ```

3. **Принудительная пересборка Docker:**
   ```bash
   docker compose build --no-cache frontend
   docker compose up -d frontend
   ```

4. **Проверьте логи на ошибки:**
   ```bash
   docker compose logs frontend | grep -i error
   ```

---

### Frontend не собирается

1. **Проверьте зависимости:**
   ```bash
   cd frontend
   npm install
   npm run build
   ```

2. **Проверьте переменные окружения:**
   - Амвера: Settings → Environment Variables
   - Railway: Variables
   - Vercel: Settings → Environment Variables

---

### База данных не обновляется

Если нужно применить миграции БД:

```bash
# Docker/Амвера
docker compose exec backend node migrate-roles.js
docker compose exec backend node migrate-quality-badge.js

# Railway CLI
railway run node backend/migrate-roles.js

# Или через Railway Shell
# Railway Dashboard → Backend → Shell → выполните команды
```

---

## 📋 Чеклист обновления

- [ ] Код закоммичен и запушен в репозиторий
- [ ] Автоматический деплой запустился (или запущен вручную)
- [ ] Деплой завершился успешно (нет ошибок в логах)
- [ ] Очищен кеш браузера
- [ ] Проверена работа фронтенда
- [ ] Проверена работа API (backend)
- [ ] Новые изменения видны на сайте

---

## 🎯 Быстрая команда для обновления Frontend (Амвера)

Если используете Амвера, самый быстрый способ:

```bash
# 1. На вашем компьютере
git add .
git commit -m "Update landing page"
git push origin main

# 2. Подождите 3-5 минут - Амвера автоматически обновит

# 3. Очистите кеш браузера и проверьте сайт
```

---

## 📞 Дополнительная помощь

Если что-то не работает:
1. Проверьте логи: `docker compose logs -f`
2. Проверьте статус контейнеров: `docker compose ps`
3. Проверьте последние коммиты: `git log --oneline -5`

